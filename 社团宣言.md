# 社团宣言 · Manifesto

---

## 原文

| Lumia | 中文 | English |
| --- | --- | --- |
| `komo li mako, mi li miko.` | 宇宙很大，我很小。 | The cosmos is vast; I am small. |
| `mi mola li mira e sela.` | 我们仰望星辰。 | We look up at the stars. |
| `meta la, mi mola li vega wo komo.` | 未来，我们奔赴宇宙。 | In the future, we journey to the cosmos. |

**连读**

> komo li mako, mi li miko.
> mi mola li mira e sela.
> meta la, mi mola li vega wo komo.

---

## 逐词

| Lumia | 拆解 |
| --- | --- |
| `komo li mako, mi li miko.` | 宇宙 `komo` ‖ 大 `mako` ， 我 `mi` ‖ 小 `miko` |
| `mi mola li mira e sela.` | 我 `mi` + 多 `mola`（= 我们）‖ 看 `mira` + 宾标 `e` + 星辰 `sela` |
| `meta la, mi mola li vega wo komo.` | 未来 `meta` + 语境 `la` ‖ 我们 ‖ 去 `vega` + 向 `wo` + 宇宙 `komo` |

**全部用词（13 个）**：`komo` `li` `mako` `mi` `miko` `mola` `mira` `e` `sela` `meta` `la` `vega` `wo`

---

## 设计说明

1. **零新词。** 三句只用既有词汇。「我们」由 `mi mola` 表达——`X mola`（X 且多）本就是既有构造。
2. **结构递进。** 第一句是单纯的对照，第二句引入施事，第三句加时间框架。三句依次变长，正好展示 Lumia 从简到繁的句子形态。
3. **第一句取自项目既定标语**，不另起炉灶。

### 一处仍然存在的空白

Lumia **没有转折连词**（但是 / 然而）。若需让步语气，用 `la` 语境框架：

```
miko la, mi li kefa.        虽渺小，我仍在思考。
```

这是刻意保留的留白——新增转折词会打破 97 词的整数，目前不作补充。

---

## 星座体版本

三句可用星座体排成三座星座（第一句为短句，首尾可闭合）。
排布规则见 `文字/星座体规范.md`。
