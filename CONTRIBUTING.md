# 贡献指南 · Contributing

欢迎贡献！Welcome! 这是一个开发版项目，改动前请先了解以下约定。

## 核心原则：数据是唯一权威源

所有语言数据都集中在 **`数据/lumia.json`**。改动流程：

1. **先改 `数据/lumia.json`**（词条、音系、语法、字形配方等）。
2. **同步 Markdown 文档**（`词典/`、`音系/`、`语法/`、`数字/`、`文字/` 等）。
3. **运行校验，确保全部 PASS**：
   ```bash
   powershell -File 数据/校验.ps1
   ```
4. 若改动了 `lumia.json`，**刷新可检索词典**：
   ```bash
   powershell -File 数据/生成词典.ps1
   ```

> **校验是机器强制的，不是提醒。** 共 34 项，其中：
> - 第 12 项：全库扫描「历史遗留写法」，包括**数词后置**（`luna dua` 应为 `dua luna`）。
> - 第 14–15 项：`script.constellation` 段必须字段完整，且其 `spec` / `example` 指向的文件必须真实存在。
> - 第 16 项：把 `文字/星座体规范.md` 里的关键数值（`0.42` / `0.27` / `26` / `17` / `≤ 4 词` / `50–90°` / `120px`）
>   与 JSON 的 `script.constellation` 逐一比对。
> - 第 17 项：SVG 结构健全性，共三条：
>   1. 每个 `<use href="#id">` 必须在**同一文件**内有对应的 `id`；
>   2. `translate()` 的参数里**不得出现千位分隔符**；
>   3. 驱动**几何 / 描边属性**（`stroke-dashoffset`、`d`、`cx`、`r` 等）的 `<animate>`
>      必须是**形状元素的子元素**——写成兄弟节点就没有目标，动画永不生效。
>   三者都会**静默失败**：空白、错位、或「只有点没有笔画」，肉眼之外极难定位。
>   > ⚠️ **踩过的坑（两条，都已由本条覆盖）**：
>   > - PowerShell 的 `"{0:N1}" -f $x` 会给 1053.1 加上千位逗号，写出
>   >   `translate(1,020.8,1,025.9)` —— 非法 SVG，节点被丢到原点。**生成坐标请用 `"{0:F1}"`**。
>   > - `<path …/><animate attributeName="stroke-dashoffset" …/>` 写成**兄弟节点**，
>   >   笔画被永久隐藏。正确写法是把 `<animate>` 放进 `<path>` **里面**。
> - 第 19–20 项：**字形覆盖**。三张词符表必须与 lexicon 的 `astro` / `func` / `base`
>   **双向精确相等**（一个不多、一个不少）；`音节图.svg` 必须正好画 16 辅音 × 5 元音 = 80 格。
> - 第 21–22 项：**句子能不能算数**。例句/常用语/星期里的每个词都必须在词典中存在
>   （拼错会渲染成通顺的废话，别处无从察觉）；`numerals.examples` 必须按组数规则**真算出**键里的数字。
> - 第 23–24 项：`README.md` 版本号与 JSON 同步；Markdown 相对链接必须都能解析。
> - 第 25 项：数据库里的每个**时态/隐喻框架**都要在 `语法/语法.md` 里有对应说明。
> - 第 26 项：≤ 4 词的短句在星座体示例里**必须画闭合轨**（对应 `closure.maxWords`）。
> - 第 27 项：本脚本**自身**的可执行代码保持纯 ASCII（见文末「编码注意」）。
> - 第 28 项：**弧半径不得小于半弦**。按 SVG 1.1 §F.6.6，此时渲染器会**静默把半径放大**，
>   于是两段本该不同的弧重合、图形悄悄失去面积，看上去却仍然「像那么回事」。
>   `luna`（月亮）就曾这样退化成零面积曲线。
>   > ⚠️ **踩过的坑**：`A 42 42 … A 34 34 …` 想画月牙，第二个半径 34 不足半弦 42，
>   > 被放大成 42 后与外弧**完全重合**。手写弧时请先算一下 `弦长 / 2 ≤ r`。
>
> 也就是说：**改了规范里的数值却忘了同步 JSON（或反过来），校验会直接 FAIL**。

## 提交建议

- 一个 PR 聚焦一件事（如「新增词」「修正音系」「补字形」）。
- 提交信息用中文或英文皆可，简洁说明改动。
- 新增词请同时补：词典条目、JSON `lexicon`、字形配方（若为实词）。

## 许可证提醒

本项目文档与字形采用 CC BY-SA 4.0。若你的贡献被合并，即表示你同意以同样许可证发布。

## 编码注意

`数据/校验.ps1`、`数据/生成词典.ps1` 的**可执行代码**保持纯 ASCII（中文路径用 Unicode 码点拼出），以规避 PowerShell 5.1 的 GBK 误读问题。修改脚本时请保持这一做法——**不要**在字符串、路径、标识符里直接写中文。

> 注释里出现中文是安全的（已实测：PowerShell 5.1 下 34 项校验全 PASS），因为注释不参与求值。但为稳妥起见，代码部分请始终用码点拼中文。
> 校验第 27 项会**自动检查这一条**：脚本里出现「非注释行的非 ASCII 字符」即 FAIL。

> 另注：Windows 自带的 Windows PowerShell 5.1 命令是 `powershell`，PowerShell 7+ 才是 `pwsh`。两个都能跑本项目的脚本。
