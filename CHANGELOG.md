# 更新日志 · Changelog

> 版本号随设计变更递增。语言本体可自由使用；文档与字形按 CC BY-SA 4.0 授权。

## [0.9.0] — 星座体：数词身份与音节串；时间表达
- **时间表达**（全部复用现有词，不新造）：
  - **小时** = `aka`（一天 `oba` 是圆，一小时是圆上的一段弧）；`ota aka` = 八点。
  - **星期** = `sema na 天体名`（七曜）；只取肉眼可见的五颗行星 + 日月，地球/天王星/海王星不入七曜。
  - `数据/lumia.json` 新增顶层键 `time`；`aka` 词条增加「一小时」义。
- 数词按**一词双义**分两种身份：**实义**作主干节点（cell 44）；**数值义**作**卫星**（cell 28），
  以「较浅的连线」挂在名词旁侧，形如地球与月球——这是星座体立体感的来源。
- 专名以**音节符串**拼写：首节坐句轨节点位，后续音节向外延伸成「双体 / 多体」（cell 30，节距 42px）。
- 新增统一公式 **光晕半径 = cell / 2 + 1**；原有的主星 26 / 伴星 17 由此导出。
- `数据/lumia.json` 新增 `cell` 与 `attachments` 参数段；校验锚点 7 → **14 项**。
- 校验脚本 18 → **23 项**：新增星座体参数段完整性、引用文件存在性、参数与规范一致性、数词后置写法、**SVG 引用有效性**五项检查。
- 修正既有文档缺陷：`luna dua` → `dua luna`（`实用语.md`、`数字.md` 各一处）；
  并补上示例中缺失的 `luna` 字形定义（漏写会静默渲染成空白）。
- 新增示例 `文字/星座体数字与音节示例.svg`。

## [0.8.0] — 星座体（星座式排布）
- 新增星座体规范 `文字/星座体规范.md` 与示例 `文字/星座体示例.svg`（三句连排）。
- 新增**星位光晕**规则（主星 r=26 / 伴星 r=17）；**句轨断口 = 光晕半径**，解决 `li` 等伴星字形与句轨混淆的问题。
- **短句闭合**（≤ 4 词首尾相连成图形）；内角目标 50–90°；空间按词数分配。
- `数据/lumia.json` 新增 `script.constellation` 参数段，`script.direction` 指向规范文档。
- 文档层：README 增「在线阅读」导航；`文字/文字.md` 内嵌 27 张星符图并新增 §十；命令 `pwsh` → `powershell`。

## [0.7.0] — 数字符号图 / 校验脚本 / 可检索词典
- 新增数字符号图 `文字/数字符表.svg`（数字改为纯位值写法）。
- 新增一致性校验脚本 `数据/校验.ps1`（18 项检查）。
- 新增可检索词典 `词典.html` + 生成脚本 `数据/生成词典.ps1`。
- 音节符定稿，明确三种书写模式（纯星符 / 纯音节符 / 混合）。

## [0.6.0] — 全局更新
- 核心词 95 → 97（新增 `aka` 弧、`koma` 尾）。
- IPA 去除长音符号；重音改为「音高/响度，不区分长短」。
- 否定改为前置；天体隐喻补「现在 / 总是」；新增比较级、序数、引语、疑问词复用。
- `malu` 释义收窄为「错的/出故障的」、`dama` 为「漆黑的/深黑的」。

## [0.5.0] — 全词星符
- 功能词 17 + 基础词 48 星符补全，全词字形齐备（`功能词符表.svg`、`基础词符表.svg`）。

## [0.4.0] — 天文词星符
- 天文·科幻借词 19 词星符补全（`天文词符表.svg`）。

## [0.3.0] — 音节图 / 符表 / 实用语
- 新增 16×5 音节图、星符总表、基础实用语。

## [0.2.0] — 数字一词双义 + 星轨体
- 数字改为一词双义（0=`vaka`、1=`sola`）。
- 文字改为「星轨体」一笔连写风格。

## [0.1.0] — 初版
- 语言定名 Lumia（光）；音系、词典、语法、数字、文字初版 + 星符示例。
