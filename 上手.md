# 五分钟上手 Lumia

> 面向新社员。目标：**五分钟内能读、能说一句、能写一个星符**。
> 完整规则见 `音系/`、`语法/`、`词典/`、`文字/`。

---

## 1. 发音（1 分钟）

**五个元音**，按拼音读即可：

| a | e | i | o | u |
| --- | --- | --- | --- | --- |
| 啊 | 诶 | 衣 | 哦 | 乌 |

**十六个辅音**：`p t k b d g f v s h m n l r w j` —— 和拼音基本一致。

**三个坑**（记住就够）：

1. **`j` 读 /j/**，像「耶」的 y，**不是** /dʒ/（不是「知」）
2. **`r` 是颤音/闪音**（类似西班牙语），而且**只出现在元音前**，词尾绝不出现 r
3. **重音永远在第一个音节**：`LU-mia`、`KO-mo`、`SE-la`

**音节结构**：`(C)V(n)` —— 没有辅音丛、没有双元音。
所以 `lumia` 读成 `lu-mi-a`（三个音节），不是「lu-mia」。

---

## 2. 记住 11 个词（2 分钟）

先记这 11 个，就能造出很多句子：

| Lumia | 意思 | Lumia | 意思 |
| --- | --- | --- | --- |
| `mi` | 我 | `li` | **谓语标记**（相当于「是/做」的分隔线） |
| `tu` | 你 | `e` | **宾语标记** |
| `ta` | 他/她/它/他们 | `o` | 呼唤、祈愿 |
| `sela` | 星 | `mira` | 看 |
| `komo` | 宇宙 | `sama` | 朋友、同伴 |
| `lumia` | 光、亮的 | | |

> `ta` **不分性别、不分单复数**——「他/她/它/他们」都是它。

---

## 3. 三个造句模板（1 分钟）

### 模板一：`X li Y.` —— X 是 Y

```
sela li lumia.        星星是亮的。
mi li sama.           我是朋友。
```

- **没有系动词**！不需要「是」，`li` 只是把主语和谓语分开。
- 修饰语放**后面**：`sela mako` = 大星（不是「大星」在前）。

### 模板二：`X li V e Y.` —— X 对 Y 做 V

```
mi mira e sela.       我看星星。
mi mira e komo.       我仰望宇宙。
```

- 宾语前必须加 `e`。

### 模板三：`o X!` —— 呼唤、祈愿

```
o tu!                 你好！（呼唤你）
o sama mi!            我的朋友啊！
```

> 注意：**没有「你好」这个固定词**，`o tu!` 就是打招呼。

---

## 4. 时间怎么说（30 秒）

```
一天     = oba（轨道 / 圆 / 一个完整循环）
一小时   = aka（这个圆上的一段弧）
八点     = ota aka
星期六   = sema na Satuna    （sema 时间 + na + 天体名）
```

**七曜**：周日 `Sola` · 周一 `Luna` · 周二 `Masa` · 周三 `Mekuria` · 周四 `Jupita` · 周五 `Venusa` · 周六 `Satuna`

> 只用肉眼可见的五颗行星 + 日月，所以地球、天王星、海王星**不进星期**。

---

## 5. 星文怎么写（30 秒）

每个词配一个「**星符**」。写的时候记住四条：

1. **一笔成字** —— 尽量一笔画完（星点可以最后点）
2. **起笔在左，收笔在右** —— 所以字与字可以顺势连写
3. **无字框** —— 不画方框，符号是开放的
4. **星点点缀** —— 关键处点一颗小星点（像星座里的亮星）

想直接看字形：打开 `文字/符表.svg`，或者 `文字/字形示例/` 里的单字。

**专名（人名、星名）怎么办？** 用「音节符」逐音节拼写：
`Vega` = `ve` + `ga`，两个音节符依次相接。见 `文字/音节图.svg`。

---

## 6. 第一句完整的问候

```
o tu!
tu bena no bena?      你好吗？（V 不 V 的正反问句）
mi bena.              我很好。
```

`tu bena no bena?` 的结构和中文「你好**不**好」一模一样——这是 Lumia 和中文最像的地方。

---

## 接下来去哪儿

| 想做什么 | 看这个 |
| --- | --- |
| 查词 | [`词典.html`](词典.html)（可检索） |
| 说几句话 | `实用语.md` |
| 搞懂语法 | `语法/语法.md` |
| 搞懂发音 | `音系/音系.md` |
| 学写字 | `文字/文字.md` |
| 写海报 / 会徽 | `文字/星座体规范.md` |
| 看社团宣言 | `社团宣言.md` |

---

## 记住这三条就不会错

1. **修饰语放后面**：`sela mako` = 大星
2. **宾语加 `e`**：`mi mira e sela.`
3. **重音在第一个音节**

其余的，边用边学。
