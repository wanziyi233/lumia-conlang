# Lumia 上手指南

> 面向新成员的最短路径：音系 → 核心词 → 基本句型 → 时间表达 → 字形入门。
> 完整规则见 `音系/`、`语法/`、`词典/`、`文字/`。

---

## 一、音系

### 元音（5）

| 字母 | IPA | 参照 |
| --- | --- | --- |
| `a` | /ɑ/ | father |
| `e` | /ɛ/ | bed |
| `i` | /i/ | see |
| `o` | /ɒ/ | hot（英式短 o） |
| `u` | /u/ | moon |

### 辅音（16）

`p t k b d g f v s h m n l r w j`

三点须注意：

1. `j` 读作 /j/，**不读** /dʒ/
2. `r` 为颤音或闪音，**仅出现于元音之前**，词尾不出现 r
3. **重音固定于首音节**：`LU-mia`、`KO-mo`、`SE-la`

### 音节结构

`(C)V(n)` —— 无辅音丛，无双元音；相邻元音各自成音节。

故 `lumia` 读作 `lu-mi-a`（三段），而非两段。

---

## 二、核心词（11）

| 词 | 义 | 词 | 义 |
| --- | --- | --- | --- |
| `mi` | 我 | `li` | 谓语标记 |
| `tu` | 你 | `e` | 宾语标记 |
| `ta` | 他 / 她 / 它 / 他们 | `o` | 呼格、祈愿 |
| `sela` | 星 | `mira` | 看 |
| `komo` | 宇宙 | `sama` | 同伴、朋友 |
| `lumia` | 光；亮的 | | |

`ta` 不区分性别，也不区分单复数。

---

## 三、基本句型

### 1. `X li Y.` —— 判断

```
sela li lumia.        星星是亮的。
mi li sama.           我是同伴。
```

两条规则：

- **无系动词。** 不需要「是」；`li` 仅分隔主语与谓语。
- **修饰语后置。** `sela mako` 意为「大星」。

### 2. `X li V e Y.` —— 及物

```
mi mira e sela.       我看星星。
mi mira e komo.       我仰望宇宙。
```

宾语前须加 `e`。

### 3. `o X!` —— 呼唤、祈愿

```
o tu!                 你好（向对方致意）。
o sama mi!            我的朋友。
```

Lumia **无独立的「你好」一词**；`o tu!` 即为问候。

---

## 四、时间表达

均复用现有词，不另造：

```
一天     = oba      （轨道 / 圆 / 一个完整循环）
一小时   = aka      （该圆上的一段弧）
八点     = ota aka
星期六   = sema na Satuna
```

**七曜**

| 周日 | 周一 | 周二 | 周三 | 周四 | 周五 | 周六 |
| --- | --- | --- | --- | --- | --- | --- |
| `Sola` | `Luna` | `Masa` | `Mekuria` | `Jupita` | `Venusa` | `Satuna` |

构词为 `sema`（时间）+ `na` + 天体名。只取肉眼可见的五颗行星与日月，
故地球、天王星、海王星**不在七曜之列**。

---

## 五、字形入门

Lumia 的文字系统称 **Selagrafi**（星文），书写规范为「星轨体」。每个词对应一个**星符**。

四条原则：

1. **一笔成字** —— 尽量以一笔连续写成（星点可最后点出）
2. **起笔在左，收笔在右** —— 故字与字可顺势连写
3. **无字框** —— 不设外框，符号开放
4. **星点点缀** —— 关键处点一颗小星点

字形查阅：`文字/符表.svg`、`文字/字形示例/`。

**专名**（人名、星名）以**音节符**逐音节拼写：`Vega` = `ve` + `ga`。
音节符表见 `文字/音节图.svg`。

---

## 六、示例对话

```
o tu!
tu bena no bena?      你好吗？
mi bena.              我很好。
```

`tu bena no bena?` 为「正反问句」，结构与汉语「你好不好」一致。
这是 Lumia 与汉语最接近的一处。

---

## 七、三条最常犯的错误

| # | 错误 | 正确 |
| --- | --- | --- |
| 1 | 修饰语前置（`mako sela`） | **后置**：`sela mako` |
| 2 | 宾语不加 `e` | `mi mira **e** sela.` |
| 3 | 重音落在非首音节 | **首音节**：`LU-mia` |

---

## 八、延伸阅读

| 目的 | 文件 |
| --- | --- |
| 查词 | `词典.html`（可检索） |
| 常用语句 | `实用语.md` |
| 语法全貌 | `语法/语法.md` |
| 音系细则 | `音系/音系.md` |
| 文字系统 | `文字/文字.md` |
| 艺术排布（海报 / 会徽） | `文字/星座体规范.md` |
| 社团宣言 | `社团宣言.md` |
